resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- literals:
  - CLUSTER_DOPPLERSECRET_NAMESPACE=doppler-operator-system
  name: controller-manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: dopplerhq/kubernetes-operator
  newTag: latest
