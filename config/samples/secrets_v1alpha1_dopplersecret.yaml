apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: dopplersecret-sample
spec:
  serviceToken: $DOPPLER_TOKEN  # Change to your Doppler Service Token
    secretName: doppler-test-secret # Kubernetes Secret name
